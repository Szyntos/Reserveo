#Kotlin
kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx3072M

#Gradle
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.caching=true

#Android
android.nonTransitiveRClass=true
android.useAndroidX=true

#Reserveo release version â€” bump both before cutting a GitHub release.
# Git tag must be v<RESERVEO_VERSION_CODE>-<RESERVEO_VERSION_NAME>, e.g. v2-1.1.0 â€” see RELEASING.md.
RESERVEO_VERSION_CODE=6
RESERVEO_VERSION_NAME=1.2.3
